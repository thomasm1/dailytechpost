import { PipeCapitalizeCategoryPipe } from './pipe-capitalize-category.pipe';

describe('PipeCapitalizeCategoryPipe', () => {
  it('create an instance', () => {
    // const pipe = new PipeCapitalizeCategoryPipe();
    // expect(pipe).toBeTruthy();
  });
});
