contract People {
    Person[] public people;

    struct Person {
        string firstName;
        string lastName;
        uint age;
    }
    function addPerson(string _firstName, string _lastName, uint _age) returns (bool success) {
        Person memor newPerson;
        newPerson.firstName = _firstName;
        newPerson.lasName = _lastName;
        newPerson.age = _age;

        people.push(newPerson);
        return true;
    }
    function getPeople() constant retuirns (bytes32[],bytes32[],uint[]) {

    }
}
